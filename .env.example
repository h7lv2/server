# --- Domains & Email ---
PRIMARY_DOMAIN=yourdomain.com

# --- Service Primary Domains ---
# These are the main domains for each service
VAULT_SUBDOMAIN=vault
PDS_SUBDOMAIN=pds
PDS_HOSTNAME=pds.yourdomain.com

# --- S-UI Domain ---
SUI_DOMAIN=your-other-domain.com
SUI_SUBDOMAIN=proxy

EMAIL=your@email.com

# --- Traefik / Cloudflare ---
# Cloudflare DNS API token with Zone:Read and DNS:Edit on the zone
CF_DNS_API_TOKEN=your_cloudflare_dns_api_token

# --- External PostgreSQL ---
# Connection URL for Tranquil PDS
DATABASE_URL=postgresql://user:password@your-pg-host:5432/tranquil_pds
# Connection URL for Vaultwarden
VAULT_DATABASE_URL=postgresql://user:password@your-pg-host:5432/vaultwarden

# --- External Object Storage (S3/MinIO) ---
BLOB_STORAGE_BACKEND=s3
# Yandex Object Storage region (required so the S3 client doesn't try IMDS)
AWS_REGION=ru-central1
S3_BUCKET=your-bucket-name
S3_ENDPOINT=https://storage.yandexcloud.net
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key

# --- Tranquil PDS secrets ---
# Generate strong random values for these
JWT_SECRET=replace_me_with_a_long_random_secret
DPOP_SECRET=replace_me_with_a_long_random_secret
MASTER_KEY=replace_me_with_a_long_random_secret
# Telegram integration (optional)
TELEGRAM_BOT_TOKEN=replace_me_if_you_enable_telegram

# --- Email / SMTP (smarthost) ---
# Required for invite codes and account verification emails.
# Recommended: Mailgun free tier (100/day, no credit card).
MAIL_FROM_ADDRESS=noreply@halvacoffee.fyi
MAIL_FROM_NAME=Tranquil PDS
MAIL_SMARTHOST_HOST=smtp.mailgun.org
MAIL_SMARTHOST_PORT=587
MAIL_SMARTHOST_USERNAME=postmaster@mg.halvacoffee.fyi
MAIL_SMARTHOST_PASSWORD=replace_me_with_mailgun_smtp_password
MAIL_SMARTHOST_TLS=starttls
